# 项目资料管理系统 PRD

## 1. 背景与问题定义
当前项目资料通常分散在会议纪要、PPT、本地文件夹、聊天记录和临时笔记中。由于缺少统一的项目视图和活动沉淀机制，用户经常面临以下问题：
- 一次讨论后没有明确结论，后续又从头讨论。
- 讨论中形成了结论，但没有被记录或没有进入持续跟踪。
- 待办只存在于纪要正文中，无法更新状态、记录进展、做项目级汇总。
- 文件很多，但缺少活动上下文，不知道哪份材料是本次活动产出的，哪份只是参考资料。
- 项目进行一段时间后，用户无法快速回答“项目当前结论是什么、下一步做什么、关键资料有哪些”。

本产品的目标不是做通用笔记软件，也不是做纯文件夹管理器，而是建立一套围绕项目推进的结构化信息闭环。

## 2. 产品目标与非目标
### 2.1 产品目标
- 以项目为容器，以活动为时间线组织单元。
- 支持在活动下记录原始内容，并提炼为关键结论与待办。
- 在项目级自动汇聚关键结论、未完成待办、标星文件。
- 支持文件的上下文关联、标签管理与项目目录映射。
- 支持 AI 对记录内容做标题建议、结论提取、待办提取。
- 让用户在任意时刻快速掌握“当前项目状态”。

### 2.2 非目标（v1 不做）
- 多人实时协作。
- 复杂权限体系与审批流。
- 在线云同步与跨设备冲突处理。
- 完整的团队项目管理看板系统。
- 高度自由的富文本样式系统。
- 复杂自动化工作流与第三方集成。

## 3. 核心原则
1. **上下文优先**：所有信息必须归属于某个项目，且尽可能归属于某次活动。
2. **结果优先**：活动不仅要留下原始记录，还要沉淀结论与待办。
3. **演进优先**：信息从“记录”逐步演进为“提炼结果”和“项目级状态”。
4. **轻记录，重沉淀**：录入过程要尽量顺滑，结构化可以稍后完成。
5. **文件不是中心**：文件服务于项目与活动，不取代项目状态本身。
6. **AI 辅助，不替代判断**：AI 产出默认为建议稿，需要用户确认。

## 4. 术语与对象模型
- **Project**：项目的最高容器；展示项目当前状态与汇总结果。
- **Activity**：项目时间线上的一次关键工作事件，如会议、法务确认、方案评审、汇报准备。
- **Note**：活动中的原始记录对象；仅保留 quick_note 与 meeting_minutes 两类。
- **Conclusion**：从活动记录中沉淀出的明确结论、共识或判断。
- **Todo**：从活动中提炼出的后续行动项，可跟踪状态与进展。
- **TodoProgress**：待办的阶段性进展记录。
- **Document**：项目或活动关联的文件材料，可标记为关键资料或参考资料，并支持标签与标星。

对象关系：
- Project 1:N Activity
- Activity 1:N Note / Conclusion / Todo / Document
- Todo 1:N TodoProgress
- Todo 必须归属于 Project，可选归属于某个 Activity
- Conclusion 允许来源于 Activity，也允许在 Project 级直接创建

## 5. 目标用户与使用场景
### 5.1 目标用户
- 复杂项目的负责人、产品经理、BA、方案经理、跨团队协调者。
- 需要大量开会、对齐、沉淀纪要、跟踪行动项的人。

### 5.2 典型场景
- 新建一个项目，并在本地工作目录生成对应文件夹。
- 在项目中新增一次活动，快速记下讨论内容，随后提炼结论与待办。
- 为活动挂接 PPT、纪要、政策文档等文件，并通过标签和标星做归类。
- 在项目首页查看所有未完成待办、关键结论和最近重点活动。
- 持续更新待办进展，在下次活动时承接上次讨论结果。

## 6. 功能需求
### 6.1 项目管理
- 支持创建项目，字段包括：项目名称、摘要（可选）、状态、根目录路径。
- 新建项目时，在用户指定工作目录下创建同名项目文件夹。
- 顶部采用项目页签形式展示活跃项目；归档项目不在页签中展示。
- 支持项目归档（archive）。v1 中归档至少实现逻辑归档 + UI 隐藏；是否物理移动目录由配置控制。
- 项目详情页顶部展示项目级汇聚结果：关键结论、未完成待办、标星文件、最近活动。
- 允许直接在项目级创建 Conclusion 和 Todo。

### 6.2 活动管理
- Activity 是项目时间线上的一次关键工作事件。
- 支持通过“新增活动”按钮创建活动；默认写入当前时间，允许修改。
- 时间字段使用单个 datetime；默认可做半小时粒度取整，但用户可精确修改。
- 创建活动时可选模板分类：财经、账务、运营、法务、合规、会议、汇报、其他。
- 活动包含字段：分类、标题、活动时间、折叠/展开状态、固定展开状态、整理状态。
- 活动按 activity_time 倒序展示；默认折叠，用户可固定展开。
- 建议引入整理状态：未整理 / 已整理。未整理活动在项目页应有明显提示。

### 6.3 Note（记录）
- 一个 Activity 下支持多条 Note。
- Note 类型仅保留两类：quick_note、meeting_minutes。
- quick_note 用于即时、轻量、过程性记录；meeting_minutes 用于正式纪要。
- meeting_minutes 在交互上默认建议“每个 Activity 0 或 1 条为主”，但数据模型可支持多条。
- 支持在活动下连续追加 quick_note，不要求每条记录都填写标题。
- meeting_minutes 可提供轻模板（建议区块：背景 / 讨论要点 / 初步结论 / 行动项），但不强制。
- Note 仅承担原始记录职责，不直接替代 Conclusion 与 Todo。

### 6.4 Conclusion（关键结论）
- 支持从 Note 中手动提炼或 AI 辅助提炼 Conclusion。
- Conclusion 用于承载明确结论、判断、共识、阶段性决定。
- 每条 Conclusion 包含：内容、来源（project/activity/note）、是否提升到项目级、创建时间、更新时间。
- 项目首页汇聚展示所有“项目级可见”的 Conclusion。
- Conclusion 不承载讨论过程，也不承载行动项。

### 6.5 Todo 与进展
- 支持从活动中手动创建 Todo，或由 AI 建议后用户采纳。
- Todo 至少包含：标题、描述（可选）、状态、优先级、截止时间（可选）、来源项目、来源活动（可选）。
- 状态枚举：todo / doing / done / blocked / cancelled。
- Todo 支持添加多条进展记录 TodoProgress；每条进展记录包括内容、时间、状态快照。
- 项目首页只汇聚未完成 Todo（todo / doing / blocked）。
- Todo 可直接在项目级创建，以支持非特定活动来源的项目任务。

### 6.6 文件管理
- 支持导入文件并关联到项目或活动。
- 文件角色区分为：关键资料（key_material）、参考资料（reference_material）。
- 支持为文件打多个标签；支持快速选择常用标签。
- 支持“标星”特殊标记；被标星的文件自动显示在项目级区域。
- 文件导入策略建议支持两种模式：仅记录原始路径；复制到项目目录并由系统接管。v1 可优先实现“引用路径”，并提供后续扩展点。
- 若文件路径失效，系统应提供明显状态提示，并允许用户重新定位文件。

### 6.7 编辑器
- 采用受控富文本编辑器，体验接近 Word，但只支持有限样式集合。
- 支持 Markdown 常见语义映射：三级标题、正文、粗体、斜体、列表、任务列表、表格、引用、链接、代码块、分隔线。
- 不支持任意字体、任意颜色、任意字号与复杂样式嵌套。
- 编辑器内部允许使用 JSON schema 或 HTML 存储；为便于 AI 与导出，建议同步生成标准化 Markdown 文本。

### 6.8 搜索与筛选
- v1 至少支持项目内搜索：标题、Note 内容、Conclusion、Todo、文件名。
- 支持按活动分类、待办状态、是否标星、文件标签进行筛选。
- 活动列表支持过滤：全部 / 有未完成待办 / 未整理 / 固定展开。

### 6.9 AI 辅助
- AI 能力定位为“提炼器”，不是自动决策器。
- 支持对单条 Note 做标题建议、结论提取、待办提取。
- 支持对整个 Activity 下的 Note 做活动级汇总建议。
- AI 输出均为建议稿，必须由用户确认采纳，不能直接覆盖正式数据。
- AI 草稿不单独作为 Note 类型存在；如果用户希望保留，可作为附件或编辑器临时草稿。

## 7. 页面与信息架构
### 7.1 顶部页签区
- 展示所有活跃项目页签。
- 支持新建项目入口、已归档项目入口。

### 7.2 项目页面布局
- 左侧：活动列表、筛选器、项目基础信息。
- 右侧主区顶部：项目摘要、关键结论、未完成待办、标星文件。
- 右侧主区中部：新增活动按钮、快速新增项目级 Todo / Conclusion 入口。
- 右侧主区下部：按时间线折叠展示活动卡片。

### 7.3 活动卡片
- 折叠态展示：时间、分类、标题、结论数、待办数、文件数、整理状态。
- 展开态展示：Note 列表、Conclusion 列表、Todo 列表、文件区、AI 操作入口。
- 支持固定展开、补记 quick_note、上传文件、添加结论与待办。

## 8. 核心交互流程
### 8.1 新建项目
1. 用户点击“新建项目”。
2. 输入项目名称，选择或确认工作目录。
3. 系统创建项目数据，并在工作目录创建同名文件夹。
4. 系统自动打开该项目页。

### 8.2 新建活动并记录
1. 用户点击“新增活动”。
2. 选择活动分类，系统自动带入当前时间。
3. 填写活动标题（可选先留空，后续由 AI 建议补全）。
4. 进入活动详情页，先写 quick_note 或上传本次资料。
5. 用户或 AI 从记录中提炼 Conclusion 和 Todo。
6. 活动被标记为“已整理”或保持“未整理”。

### 8.3 更新待办进展
1. 在项目首页或活动内打开某条 Todo。
2. 修改状态，或新增一条进展记录。
3. 系统刷新项目级未完成待办汇总。

### 8.4 关联文件
1. 在项目或活动中点击“添加文件”。
2. 选择文件并指定角色（关键资料 / 参考资料）。
3. 选择标签，必要时勾选标星。
4. 系统记录文件关联；若配置为“复制导入”，则同步拷贝到项目目录。

## 9. 数据模型（实现建议）
推荐采用 SQLite 作为结构化数据存储，文件本体留在文件系统。
- **Project**: id, name, status, root_path, summary, created_at, updated_at
- **Activity**: id, project_id, category, title, activity_time, is_pinned, is_expanded, organize_status, created_at, updated_at
- **Note**: id, project_id, activity_id, note_type, title, content_raw, content_rendered, created_at, updated_at
- **Conclusion**: id, project_id, activity_id(nullable), note_id(nullable), content, promoted_to_project, created_at, updated_at
- **Todo**: id, project_id, activity_id(nullable), source_note_id(nullable), title, description, status, priority, due_date(nullable), created_at, updated_at
- **TodoProgress**: id, todo_id, content, status_snapshot, created_at
- **Document**: id, project_id, activity_id(nullable), name, path, storage_mode, mime_type, role, is_starred, created_at, updated_at
- **Tag**: id, name, category, created_at
- **DocumentTag**: document_id, tag_id

关键约束：
- Todo 必须属于 Project，可选属于 Activity。
- Conclusion 可属于 Project 或 Activity；若来源于 Note，则保留 note_id。
- Note 仅支持 quick_note / meeting_minutes 两种类型。
- Project、Activity、Todo、Document 至少需要 updated_at。

## 10. 技术架构建议
- 客户端形态：桌面应用，优先考虑 Electron 或 Tauri。
- 结构化数据：SQLite。
- 文件数据：本地文件系统；项目目录与系统数据映射。
- 编辑器：Tiptap / ProseMirror 或等价可控富文本框架。
- 状态管理：按项目维度做本地缓存与索引；允许离线使用。
- 搜索：v1 可基于 SQLite FTS 或简化的本地索引实现。
- AI：封装为服务层，输入 Note / Activity，输出 suggestion，不直接落正式表。

## 11. MVP 验收标准
- 用户可以创建项目，并在本地目录生成项目文件夹。
- 用户可以在项目中创建活动，并看到按时间线倒序排列的活动列表。
- 一个活动下可创建多条 quick_note，以及至少一条 meeting_minutes。
- 用户可以在活动中手动新增 Conclusion 和 Todo，并在项目首页看到汇总结果。
- Todo 支持状态切换与进展记录；项目首页仅展示未完成 Todo。
- 用户可以导入文件、标记关键资料/参考资料、添加标签与标星；标星文件能在项目级展示。
- 用户可以搜索项目内标题、记录内容、结论、待办和文件名。
- AI 能够基于 Note 生成标题/结论/待办建议，且需要用户确认后才入库。

## 12. 版本规划建议
- **v1.0**：项目 / 活动 / Note / Conclusion / Todo / Document 基础闭环；本地优先；搜索；基础 AI 提炼。
- **v1.1**：导出 Markdown / 项目摘要；项目级周报生成；待整理活动提醒；文件失效修复。
- **v1.2**：更强的标签体系；更多筛选视图；活动模板增强；阶段总结。
- **v2.0**：云同步、多端、协作能力、权限与评论体系。

## 13. 给 CodeX 的实现说明
- 先实现单用户、本地优先，不引入服务端依赖。
- 把 Project / Activity / Note / Conclusion / Todo / Document 作为一等对象建模，不要把它们混成一个通用内容表。
- 不要把 Note 与 Conclusion / Todo 混在同一编辑区做“富文本内嵌解析”，应保留明确的数据边界。
- Todo 的状态与进展必须是结构化字段，不要只写在文本纪要里。
- Activity 默认折叠，但支持固定展开；项目首页优先展示汇总，不展示全量正文。
- AI 输出必须走 suggestion 流，不得自动覆盖正式数据。
- 编辑器样式受控，不做自由排版。

**文档结论**：这是一个“项目推进工作台”，不是纯笔记工具。其核心竞争力在于强制每次活动形成可沉淀输出，并在项目级持续汇聚当前状态。







