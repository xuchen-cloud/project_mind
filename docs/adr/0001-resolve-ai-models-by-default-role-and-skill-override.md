# Resolve AI models by default role and Skill override

AI configuration keeps reusable model Profiles but exposes only a general default and an image default. An AI Editor Skill may override either target's default with one Profile; this avoids capability-specific global bindings while keeping common setup simple and specialized Skills predictable.
