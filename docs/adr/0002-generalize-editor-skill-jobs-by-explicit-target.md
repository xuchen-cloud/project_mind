# Generalize Editor Skill Jobs by explicit target

Text selections and individual images run through one Editor Skill Job pipeline with an explicit target. Sharing model resolution, prompt trust boundaries, lifecycle, concurrency protection, preview handling, and Markdown write-back prevents image interpretation from becoming a parallel subsystem with different safety or rollback behavior.
